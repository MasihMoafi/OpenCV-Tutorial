# مستندات فصل ۳: هیستوگرام‌ها و پردازش رنگ تصاویر

## خلاصه
این فایل شامل توابع مختلف برای محاسبه و نمایش هیستوگرام‌های تصویر، یکسان‌سازی هیستوگرام، و خوشه‌بندی رنگ‌ها با استفاده از الگوریتم K-means است.

---

## توابع کمکی

### 1. تابع `show(images)`

**ورودی:**
- `images`: لیستی از تاپل‌ها، هر تاپل شامل:
  - `str`: عنوان پنجره نمایش
  - `cv2.typing.MatLike`: تصویر به فرمت OpenCV

**خروجی:**
- هیچ (نمایش تصاویر در پنجره‌های OpenCV)

**فرآیند:**
1. برای هر تصویر در لیست، یک پنجره با عنوان مشخص ایجاد می‌کند
2. تصویر را نمایش می‌دهد
3. منتظر فشردن هر کلیدی توسط کاربر می‌ماند
4. تمام پنجره‌ها را می‌بندد

**کاربرد:** نمایش چندین تصویر به صورت همزمان برای مقایسه

---

### 2. تابع `plot(hists)`

**ورودی:**
- `hists`: لیستی از تاپل‌ها، هر تاپل شامل:
  - `str`: عنوان نمودار
  - `str`: برچسب محور افقی (X)
  - `str`: برچسب محور عمودی (Y)
  - `np.ndarray`: داده‌های هیستوگرام
  - `tuple`: رنگ‌های خطوط (برای هیستوگرام‌های چند کاناله)

**خروجی:**
- هیچ (نمایش نمودارها در matplotlib)

**فرآیند:**
1. یک شکل (figure) با تعداد زیرنمودار (subplot) برابر با تعداد هیستوگرام‌ها ایجاد می‌کند
2. برای هر هیستوگرام:
   - اگر رنگ‌ها مشخص شده باشد، برای هر کانال یک خط با رنگ مشخص رسم می‌کند
   - در غیر این صورت، یک خط سیاه رسم می‌کند
   - عنوان و برچسب‌های محورها را تنظیم می‌کند
3. نمودارها را نمایش می‌دهد

**کاربرد:** نمایش چندین هیستوگرام به صورت همزمان برای تحلیل و مقایسه

---

## توابع محاسبه هیستوگرام

### 3. تابع `compute_1D_histogram(image, bins=256)`

**ورودی:**
- `image`: آرایه numpy با شکل `[height, width]` (تصویر خاکستری)
- `bins`: تعداد سطل‌های هیستوگرام (پیش‌فرض: ۲۵۶)

**خروجی:**
- آرایه numpy با شکل `[bins]` حاوی فراوانی هر سطح روشنایی

**فرآیند:**
1. ضریب کوانتیزاسیون را محاسبه می‌کند: `factor = 256 // bins`
2. یک آرایه صفر با اندازه `bins` ایجاد می‌کند
3. برای هر پیکسل در تصویر:
   - مقدار روشنایی را می‌خواند
   - شاخص کوانتیزه شده را محاسبه می‌کند: `quantized_index = intensity // factor`
   - فراوانی آن شاخص را یک واحد افزایش می‌دهد
4. آرایه هیستوگرام را برمی‌گرداند

**توضیحات اضافی:**
- این تابع هیستوگرام را به صورت دستی محاسبه می‌کند
- راه‌حل‌های جایگزین با NumPy و OpenCV در کامنت قرار داده شده است

**کاربرد:** محاسبه توزیع روشنایی پیکسل‌ها در تصاویر خاکستری

---

### 4. تابع `compute_3D_histogram(image, bins=256)`

**ورودی:**
- `image`: آرایه numpy با شکل `[height, width, channels]` (تصویر رنگی)
- `bins`: تعداد سطل‌های هیستوگرام (پیش‌فرض: ۲۵۶)

**خروجی:**
- آرایه numpy با شکل `[3, bins]` حاوی هیستوگرام هر کانال رنگی

**فرآیند:**
1. ضریب کوانتیزاسیون را محاسبه می‌کند: `factor = 256 // bins`
2. یک آرایه صفر با شکل `[3, bins]` ایجاد می‌کند
3. برای هر کانال رنگی (۰، ۱، ۲):
   - برای هر پیکسل در آن کانال:
     - مقدار شدت را می‌خواند
     - شاخص کوانتیزه شده را محاسبه می‌کند
     - فراوانی آن شاخص را افزایش می‌دهد
4. آرایه هیستوگرام سه کاناله را برمی‌گرداند

**توضیحات اضافی:**
- این تابع هیستوگرام جداگانه برای هر کانال رنگی محاسبه می‌کند
- راه‌حل‌های جایگزین با NumPy و OpenCV در کامنت قرار داده شده است

**کاربرد:** تحلیل توزیع رنگ در تصاویر رنگی (BGR, YUV, HLS و غیره)

---

### 5. تابع `smoothed_histogram(image)`

**ورودی:**
- `image`: آرایه numpy (تصویر خاکستری)

**خروجی:**
- آرایه numpy با شکل `[256]` حاوی هیستوگرام هموارشده

**فرآیند:**
1. هیستوگرام اصلی را محاسبه می‌کند
2. یک کپی از هیستوگرام ایجاد می‌کند
3. برای هر نقطه (به جز اول و آخر):
   - میانگین آن نقطه و دو همسایه‌اش را محاسبه می‌کند
   - `smoothed_hist[i] = (hist[i-1] + hist[i] + hist[i+1]) / 3`
4. هیستوگرام هموارشده را برمی‌گرداند

**کاربرد:** کاهش نویز و ناهمواری‌های هیستوگرام برای تحلیل بهتر

---

### 6. تابع `histogram3D(image)`

**ورودی:**
- `image`: آرایه numpy (تصویر BGR رنگی)

**خروجی:**
- هیچ (نمایش نمودار سه‌بعدی)

**فرآیند:**
1. هیستوگرام سه‌بعدی با ۸×۸×۸ سطل محاسبه می‌کند
2. یک شبکه سه‌بعدی از مختصات ایجاد می‌کند
3. داده‌ها را به فرمت مسطح تبدیل می‌کند
4. مقادیر را نرمال‌سازی می‌کند (تقسیم بر ماکزیمم)
5. رنگ‌های مناسب برای هر نقطه محاسبه می‌کند
6. نمودار پراکندگی سه‌بعدی ایجاد می‌کند با:
   - محور X: آبی (Blue)
   - محور Y: سبز (Green)
   - محور Z: قرمز (Red)
   - اندازه نقاط: متناسب با فراوانی

**کاربرد:** تجسم سه‌بعدی توزیع رنگ در تصویر

---

## توابع پردازش تصویر

### 7. تابع `equalization(image)`

**ورودی:**
- `image`: آرایه numpy (تصویر BGR رنگی)

**خروجی:**
- آرایه numpy (تصویر BGR با کنتراست بهبود یافته)

**فرآیند:**
1. تصویر را از BGR به HLS تبدیل می‌کند
2. هیستوگرام کانال روشنایی (Luminance) را محاسبه می‌کند
3. یک جدول تبدیل (lookup table) ایجاد می‌کند:
   - برای هر سطح خاکستری ورودی (۰-۲۵۵):
     - تعداد کل پیکسل‌های دیده شده تا کنون را جمع می‌کند
     - سطح خاکستری جدید را محاسبه می‌کند: `new_output = total_pixels_so_far × 256 / (total_pixels + 1)`
     - میانگین با سطح قبلی را در جدول ذخیره می‌کند
4. برای هر پیکسل در تصویر:
   - مقدار روشنایی را با استفاده از جدول تبدیل جایگزین می‌کند
5. تصویر را از HLS به BGR برمی‌گرداند

**اصل کار:**
این تابع از **Histogram Equalization** استفاده می‌کند که توزیع روشنایی را یکنواخت‌تر می‌کند و کنتراست تصویر را افزایش می‌دهد.

**کاربرد:** بهبود کنتراست تصاویر تاریک یا با نور ضعیف

---

### 8. تابع `kmeans(image, n_clusters=10, max_iterations=1000, random_state=0)`

**ورودی:**
- `image`: آرایه numpy (تصویر BGR رنگی)
- `n_clusters`: تعداد خوشه‌ها (پیش‌فرض: ۱۰)
- `max_iterations`: حداکثر تعداد تکرار (پیش‌فرض: ۱۰۰۰)
- `random_state`: بذر تصادفی برای تکرارپذیری (پیش‌فرض: ۰)

**خروجی:**
- تاپلی شامل:
  - `centroids`: آرایه numpy با شکل `[n_clusters, 3]` حاوی مراکز خوشه‌ها
  - `labels`: آرایه numpy حاوی برچسب خوشه برای هر پیکسل

**فرآیند:**
1. بذر تصادفی را تنظیم می‌کند
2. پیکسل‌ها را به آرایه دوبعدی تبدیل می‌کند: `[تعداد_پیکسل‌ها, 3]`
3. `n_clusters` پیکسل تصادفی را به عنوان مراکز اولیه انتخاب می‌کند
4. تا رسیدن به حداکثر تکرار یا همگرایی:
   - فاصله هر پیکسل تا همه مراکز را محاسبه می‌کند
   - هر پیکسل را به نزدیک‌ترین مرکز اختصاص می‌دهد
   - مراکز جدید را با میانگین پیکسل‌های هر خوشه محاسبه می‌کند
   - اگر مراکز تغییر نکردند، حلقه را متوقف می‌کند
5. مراکز نهایی و برچسب‌ها را برمی‌گرداند

**اصل کار:**
الگوریتم **K-Means** رنگ‌های مشابه را در خوشه‌هایی گروه‌بندی می‌کند و به بخش‌بندی رنگی تصویر کمک می‌کند.

**کاربرد:**
- کاهش تعداد رنگ‌ها در تصویر (Color Quantization)
- بخش‌بندی تصویر بر اساس رنگ (Image Segmentation)

---

## بخش اجرایی برنامه

در انتهای فایل، کد اجرایی زیر قرار دارد:

```python
# خواندن تصاویر
gray_image = cv2.imread("...chess.jpg", cv2.IMREAD_GRAYSCALE)
bgr_image = cv2.imread("...lena.jpg")
dark_bgr_image = cv2.imread("...dark-image.jpeg")

# تبدیل فضای رنگی
yuv_image = cv2.cvtColor(bgr_image, cv2.COLOR_BGR2YUV)
hls_image = cv2.cvtColor(bgr_image, cv2.COLOR_BGR2HLS)

# اعمال K-means و بخش‌بندی تصویر
centroids_, labels_ = kmeans(bgr_image, n_clusters=10, max_iterations=1000)
segmented_image = centroids_[labels_].reshape(bgr_image.shape).astype(np.uint8)

# نمایش تصاویر اصلی و بخش‌بندی شده
show([
    ("Main Image", bgr_image),
    ("Segmented Image", segmented_image),
])

# نمایش هیستوگرام‌های مختلف
plot([
    ("Grayscale Histogram", ..., compute_1D_histogram(gray_image), ()),
    ("Grayscale Smoothed Histogram", ..., smoothed_histogram(gray_image), ()),
    ("BGR Histogram", ..., compute_3D_histogram(bgr_image), ("b", "g", "r")),
    ("YUV Histogram", ..., compute_3D_histogram(yuv_image), ("y", "c", "m")),
    ("HLS Histogram", ..., compute_3D_histogram(hls_image), ("g", "r", "black")),
])

# نمایش هیستوگرام سه‌بعدی
histogram3D(bgr_image)
```

**جریان کار:**
1. ✅ بارگذاری تصاویر (خاکستری و رنگی)
2. ✅ تبدیل تصویر به فضاهای رنگی YUV و HLS
3. ✅ اعمال خوشه‌بندی K-means با ۱۰ خوشه
4. ✅ ایجاد تصویر بخش‌بندی شده با جایگزینی هر پیکسل با مرکز خوشه متناظر
5. ✅ نمایش تصویر اصلی و بخش‌بندی شده
6. ✅ نمایش هیستوگرام‌های مختلف (خاکستری، هموارشده، BGR، YUV، HLS)
7. ✅ نمایش هیستوگرام سه‌بعدی رنگی

---

## نکات مهم

1. **مسیر فایل‌ها:** مسیرهای تصاویر در کد به صورت ثابت (hardcoded) نوشته شده است و باید برای اجرا تغییر داده شود.

2. **راه‌حل‌های جایگزین:** برای اکثر توابع، راه‌حل‌های کارآمدتر با NumPy و OpenCV در کامنت‌ها آورده شده است.

3. **فضاهای رنگی:**
   - **BGR**: فرمت پیش‌فرض OpenCV (Blue-Green-Red)
   - **YUV**: Y = روشنایی، U و V = اطلاعات رنگ
   - **HLS**: Hue (رنگ)، Lightness (روشنایی)، Saturation (اشباع)

4. **کاربردهای عملی:**
   - تحلیل کنتراست و روشنایی تصاویر
   - بهبود تصاویر کم‌نور
   - بخش‌بندی تصویر بر اساس رنگ
   - فشرده‌سازی رنگی

5. **پیچیدگی محاسباتی:**
   - توابع دستی (manual loops): پیچیدگی زمانی بالا، مناسب آموزشی
   - توابع NumPy/OpenCV: بهینه‌سازی شده، مناسب استفاده عملی

---

## خلاصه توابع

| تابع | ورودی | خروجی | کاربرد |
|------|-------|--------|---------|
| `show` | لیست تصاویر | نمایش | نمایش تصاویر |
| `plot` | لیست هیستوگرام‌ها | نمودار | رسم هیستوگرام‌ها |
| `compute_1D_histogram` | تصویر خاکستری | آرایه ۱D | هیستوگرام تک‌کانال |
| `compute_3D_histogram` | تصویر رنگی | آرایه ۲D | هیستوگرام سه‌کانال |
| `smoothed_histogram` | تصویر خاکستری | آرایه ۱D | هیستوگرام هموارشده |
| `histogram3D` | تصویر BGR | نمودار ۳D | تجسم سه‌بعدی رنگ |
| `equalization` | تصویر BGR | تصویر BGR | بهبود کنتراست |
| `kmeans` | تصویر BGR | مراکز و برچسب‌ها | خوشه‌بندی رنگی |
