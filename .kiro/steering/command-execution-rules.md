BEFORE any action, MUST:
1. Query memories for relevant context
2. Check if user gave EXPLICIT instruction (e.g., "use gh")
3. Verify command syntax (no 'cd', use 'path' param)
4. Check if resource already exists before creating
